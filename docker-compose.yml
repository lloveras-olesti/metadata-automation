services:
  pipeline:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
    # Override command when running a specific script:
    # docker-compose run --rm pipeline python scripts/classify_urls.py
    command: [ "python", "--version" ]
